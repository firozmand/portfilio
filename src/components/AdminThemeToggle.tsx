"use client";

import { FiMoon } from "react-icons/fi";

export default function AdminThemeToggle() {
  // Night mode only; this is just a status pill for consistency with the design.
  return <span className="admin-badge"></span>;
}
